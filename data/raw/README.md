# Raw Input Rights

`BiOX_PFM.xlsx` and `BiOX_FEA.zip` are supplied input snapshots used for the project audit. Before publishing this repository, confirm that their author, publisher, or source license permits redistribution. Remove them from the public repository if that permission is not available; the processed JSON/CSV tables and figures are kept separately so the code package can remain usable.
